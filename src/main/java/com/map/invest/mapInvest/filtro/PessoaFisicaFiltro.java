package com.map.invest.mapInvest.filtro;

public class PessoaFisicaFiltro implements Filtro {
    @Override
    public boolean equals(Object obj) {
        if (obj == null) return true; // Inclui se for null
        // Lógica adicional se necessário
        return false;
    }
}
